/*
import 'package:flutter/material.dart';
import 'package:healthapp/SwipeableWidget.dart';
import 'MenuFunction.dart';

class StressTestResult extends StatefulWidget {
  StressTestResultState createState() => StressTestResultState();
}

class StressTestResultState extends State<StressTestResult> with MenuFunction {
  @override
  Widget build(BuildContext context) {
    return SwipeableWidget(
        height: double.infinity,
        onSwipeCallback: () {
          returnBack(context);
        },
    child: SafeArea(
      child: Column(
      mainAxisAlignment: MainAxisAlignment.spaceBetween,
      crossAxisAlignment: CrossAxisAlignment.stretch,
      children: <Widget>[

        ],
    );
}
*/